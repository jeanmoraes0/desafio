a) A lógica é adicionar 2 ao número anterior em cada termo da sequência. O próximo elemento é 9.

b) A lógica é multiplicar cada termo anterior por 2. O próximo elemento é 128.

c) A lógica é elevar cada número natural ao quadrado. O próximo elemento é 49.

d) A lógica é elevar cada número par ao quadrado. O próximo elemento é 100.

e) A lógica é somar os dois termos anteriores em cada termo da sequência. O próximo elemento é 13.

f) A lógica é todos os números começam com a letra D. O próximo elemento é 200